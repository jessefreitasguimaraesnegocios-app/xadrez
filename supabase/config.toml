project_id = "wzairrwkccneltkhgztx"

[functions.create-match]
verify_jwt = false

[functions.create-pix-deposit]
# Deixamos a verificação de JWT por conta do código da função (auth.getUser()).
verify_jwt = false

[functions.request-withdrawal]
# JWT validado no código da função (auth.getUser()); gateway não bloqueia o request.
verify_jwt = false

[functions.cancel-withdrawal]
# JWT validado no código da função (auth.getUser()).
verify_jwt = false

[functions.process-my-withdrawals]
# Chamado pelo app após solicitar saque (JWT); processa PIX do usuário na hora.
verify_jwt = false

[functions.process-withdrawal]
# Chamado por cron (Bearer CRON_SECRET); validação no código da função.
verify_jwt = false

[functions.sync-failed-withdrawals]
# Sincroniza saques marcados completed com status no Asaas; devolve saldo se transferência falhou. Cron (Bearer CRON_SECRET).
verify_jwt = false

[functions.asaas-webhook]
# Webhook é chamado pelo Asaas (servidor), não pelo front — não envia JWT do Supabase.
# Sem isso o gateway retorna 401 "Missing authorization header" antes do handler rodar.
# A validação do Asaas (token) fica no código da função (ASAAS_WEBHOOK_SECRET).
verify_jwt = false

[functions.asaas-withdraw-validate]
# Autorização externa de saque: Asaas chama esta URL (não envia JWT).
# Sem verify_jwt = false o gateway retorna 401 antes do handler rodar.
# Validação opcional no código via header asaas-access-token e ASAAS_WITHDRAW_VALIDATE_TOKEN.
verify_jwt = false

[functions.make-move]
# JWT validado no código da função (auth.getUser()); gateway não bloqueia o request.
verify_jwt = false

[functions.finish-game]
# JWT validado no código da função (auth.getUser()).
verify_jwt = false

[functions.admin-report]
# JWT validado no código; apenas is_admin acessa.
verify_jwt = false

[functions.admin-tournaments]
# JWT validado no código; apenas is_admin acessa.
verify_jwt = false